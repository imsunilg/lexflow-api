# Controllers

ASP.NET Core MVC controllers, one per module, added in Phase C of the Build
Playbook. Every action must carry an authorization policy attribute — CI fails
unattributed endpoints per PRD §20(4).
