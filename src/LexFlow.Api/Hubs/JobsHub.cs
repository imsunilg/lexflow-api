using Microsoft.AspNetCore.SignalR;

namespace LexFlow.Api.Hubs;

/// <summary>Stub for /hubs/jobs (PRD §16 Realtime) — background job progress (OCR, imports, AI jobs).</summary>
public sealed class JobsHub : Hub;
