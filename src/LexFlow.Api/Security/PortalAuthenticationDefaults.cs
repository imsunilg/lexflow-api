namespace LexFlow.Api.Security;

/// <summary>Module 17: the named JWT bearer scheme portal controllers authenticate against — kept fully separate from the default ("staff"/"apikey") scheme.</summary>
public static class PortalAuthenticationDefaults
{
    public const string AuthenticationScheme = "Portal";
}
