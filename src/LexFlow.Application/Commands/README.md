# Commands

MediatR `IRequest`/`IRequestHandler` pairs for state-changing operations (one folder
per module added in Phase C, e.g. `Leads/ConvertLeadCommand.cs`). Validators for each
command live alongside it or under `Validators/`.
