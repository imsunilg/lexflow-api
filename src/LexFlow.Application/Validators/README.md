# Validators

FluentValidation `AbstractValidator<T>` classes, auto-discovered and registered by
`AddValidatorsFromAssembly` in `DependencyInjection.cs`, and run automatically by
`ValidationBehavior` in the MediatR pipeline.
