# LexFlow.IntegrationTests

xUnit + FluentAssertions + Testcontainers.PostgreSql. Every repository and API
slice against a real Postgres container (PRD §37). Redis/Elasticsearch
containers added as those integrations are exercised.
